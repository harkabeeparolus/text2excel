[tool.poetry]
name = "csv2xlsx"
version = "0.1.0"
description = ""
authors = ["Fredrik Mellström <fredrikm@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"
openpyxl = "^3.0.4"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
